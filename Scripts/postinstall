#!/bin/bash
set -euo pipefail

PRODUCT_NAME="CangjieX"
LEGACY_PRODUCT_NAME="Yahoo! KeyKey"

stop_input_method() {
    local process_name="$1"

    /usr/bin/killall "${process_name}" >/dev/null 2>&1 || true
}

stop_input_method "${LEGACY_PRODUCT_NAME}"
stop_input_method "${PRODUCT_NAME}"

echo "${PRODUCT_NAME} has been installed."
echo "Please log out and back in, then enable ${PRODUCT_NAME} in System Settings > Keyboard > Input Sources."

exit 0
